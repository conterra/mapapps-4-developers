
import { type Messages } from "../bundle";

const i18n = {
    bundleName: "camera",
    bundleDescription: "",
    ui: {
        windowTitle: "Camera"
    },
    tool: {
        title: "Camera",
        tooltip: "Camera"
    }
} satisfies Messages;

export default i18n;
